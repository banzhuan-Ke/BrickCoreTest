"""
问答评判 Prompt（RAG 评测系统 版本2：分项公式 + 5 级等级映射，已在生产验证）

Jinja2 变量（与评测用例字段对应）：
  question      — 测试问题
  ground_truth  — 标准答案（+ 可选要点补充）
  answer        — 实际回答 / 被测 API 返回的模型回答
"""

QA_JUDGE_USER_PROMPT_TEMPLATE = r"""请严格按照以下“分项公式计算+5级等级映射”评估答案相关性，所有场景标准一致，分数范围保持0.0000-1.0000。

<|question|>{{question}}<|/question|>
<|ground_truth|>{{ground_truth}}<|/ground_truth|>
<|answer|>{{answer}}<|/answer|>

**评估流程（严格按顺序）：**
1. 范畴分析 → 2. 分项公式计算（A≠0时执行） → 3. 5级等级映射 → 4. 输出结果
补充：若范畴分析后A=0.0000（范畴比例0%），直接跳过“公式计算+等级映射”，最终总分为0.0000，等级为“不合格”。

## 一、范畴一致性分析（最高优先级）
1. 定义目标类型：从问题中提取"核心查询对象"（例：问题问"缓冲块设计手册中技术评审报告内容有什么？"→目标类型="技术评审报告包含的维度/评审项"；问题问"总结了哪些经验教训"→目标类型="经验教训的总结内容"；问题问"有哪些测试项目"→目标类型="具体测试项目名称/类别"）；
2. 通用范畴识别方法（适用于所有目标类型，必须严格遵守）：
   - 有效要点（M）：所有"直接回答问题核心查询对象"的要点（即明确说明"目标类型是什么/包含什么/要求什么"的内容），均算有效；
   - 非范畴要点：仅当要点"明确属于其他独立范畴"（与核心查询对象无关，如问题问"报告内容"却讲"写报告的流程"，问题问"经验教训"却讲"预防措施"），才不算有效；
   - 特别说明：**"生成答案条目数量少于标准答案""内容未完全覆盖标准答案"属于"信息完整性问题"，与"范畴是否正确"无关**，不得因内容覆盖不足判定为"非范畴要点"；
   - 示例：
     - 问题问"技术评审报告内容有什么"→要点"公司概况（100%）""经验（100%）"均算有效（直接回答"内容包含什么"）；
     - 问题问"经验教训"→要点"充气验证流程"不算有效（属于"预防措施"，是其他范畴）；
     - 问题问"缓冲块关键设计输入需求有哪些"→要点"固定方式及密度要求""载荷-变形曲线"均算有效（直接回答"设计输入需求包含什么"，即使未覆盖所有输入点）；
     - 问题问"测试项目"→要点"力-位移测试""拉脱力测试"均算有效（直接回答"测试项目名称"）；
3. 要点计数：
   - 总要点N = 生成答案中独立编号/段落数（无编号则按"分点表述"拆分，如"1.XX 2.XX""一、XX 二、XX"）；
   - 有效要点M = 严格符合"通用范畴识别方法"的要点数（非范畴要点即使正确，也不计入M）；
   - 范畴比例 = round((M/N)×100, 2) %（N=0时按"有效字数/总字数×100%"估算，结果保留4位小数）；
   - 补充：N=0时，"有效字数"指直接回答核心查询对象的字数，"总字数"指生成答案的总字数；
   - 补充：若范畴比例=0.00%（即M=0或有效字数占比0%），则A=0.0000，触发"直接判总分0+等级不合格"规则。

## 二、分项评分计算（仅当A≠0.0000时执行）
最终总分（未映射等级前）= min(max(A + B + C_acc + D, 0.0000), 1.0000)，所有分项得分保留4位小数。

### A. 范畴一致性得分（0-0.4分）
100%→0.4000 | 90%→0.3600 | 80%→0.3200 | 70%→0.2800 | 60%→0.2400
50%→0.2000 | 40%→0.1600 | 30%→0.1200 | 20%→0.0800 | 10%→0.0400 | 0%→0.0000
补充：范畴比例非10%整数倍时，按线性插值计算（如95%→0.3800，85%→0.3400）。

### B. 信息完整性得分（最终范围-0.2000~0.3000）
1. 核心定义：
   - K = 参考答案关键信息点数（列表条目/核心维度/核心数据/责任人/时间节点，按"通用范畴识别方法"提取）；
   - C_cover = 有效范畴内覆盖的关键信息点数；
2. 计算规则：
   - 基础分 = round(0.3 × (C_cover/K), 4)（K=0时基础分=0.3000）；
   - **扣分规则（总扣分保留4位小数，严格执行）：**
     - 遗漏关键信息点：每遗漏1个扣0.0500；
     - 遗漏次要信息点：每遗漏1个扣0.0200；
     - **错误添加无依据信息：每错误添加1个独立事实扣0.0300（即使该信息本身正确但与参考答案无关）；**
   - **最终得分 = round(max(基础分 - 总扣分, -0.2000), 4)；**
3. **关键要求：**
   - **必须逐条对比生成答案与参考答案，确保C_cover计数准确；**
   - **生成答案中每个参考答案未提及的独立事实，均计为1个无依据信息；**
   - **对于模糊表述（如"FEA或样件处理"vs"FEA分析及手工样件试制"），若未明确包含关键信息，视为遗漏；**
4. 特殊规则：范畴比例<20%时，B的最终得分需乘以0.5（即round(B×0.5,4)）。

### C_acc. 准确性得分（最终范围-0.2000~0.2000）
1. 基础分：0.2000；
2. 扣分规则（总扣分保留4位小数，扣至-0.2000止）：
   - **轻微错误（数据偏差<10%、表述不严谨、符号乱码但可识别）：每处扣0.0500；**
      - **注意1：乱码仅在C_acc扣分，不在D分（表达质量）中重复扣分；**
      - **注意2：同一乱码片段（如连续乱码字符）算作1处乱码错误，不按字符拆分；**
   - **明显错误（偏差≥10%、数值错误、名称/时间错误、逻辑矛盾、关键单位错误、术语解释错误）：每处扣0.1000；**
      - **数值错误判定：将生成答案中的数值与参考答案对应数值比较，偏差≥10%即为明显错误；**
      - **精确计算偏差公式：偏差 = |生成答案数值 - 参考答案数值| ÷ max(参考答案数值, 0.01) × 100%；**
      - **示例：参考答案为2.56mm，生成答案为0.3mm，偏差 = |0.3-2.56|÷2.56×100%=88.28% ≥10%，属明显错误；**
      - **术语解释错误：如将"SOR"解释为"Sales Order Requirement"而非"Statement of Requirements"，属明显错误；**
   - 严重错误（关键信息完全错漏、误导性表述、虚构不存在的信息）：每处扣0.1500；
3. 最终得分 = round(max(0.2000 - 总扣分, -0.2000), 4)；
4. 特殊规则：
   - **必须明确列出所有错误类型和具体数量，格式为"错误类型=[类型1(数量), 类型2(数量), ...]"，无错误则写"错误类型=无错误，错误数量=0"；**
   - 范畴比例<20%时，C_acc的最终得分需乘以0.5（即round(C_acc×0.5,4)）；
   - 财务/安全/法律/医疗健康/个人信息/核心技术数据/食品安全/金融监管/环境保护等高敏感问题：若C_acc<0（负分），最终总分不得超过0.3000。。

### D. 表达质量得分（0-0.1分，加分项）
- 结构清晰（有编号/标题/分段）：+0.0400；
- 逻辑连贯（先总后分/因果关系明确/无矛盾）：+0.0300；
- **语言规范（符号正确/术语准确/无其他表达问题）：+0.0300；**
   - **注意：此项评估时不考虑已在C_acc扣分的乱码，仅评估其他语言规范问题；**
   - **如果仅有乱码问题，没有其他语言不规范问题，此项仍可得0.0300分；**
补充：每项符合则加对应分值，不符合则加0，最高累计0.1000分。

## 三、新增：5级等级映射规则（优化分数范围，提升区分度）
### 1. 等级划分标准（0~1分数对应5级，无主观调整）
| 等级名称 | 等级描述（质量水平）       | 0~1分数范围（含边界值） | 核心判定依据（与分数范围匹配，辅助验证）                          |
|----------|----------------------------|-------------------------|---------------------------------------------------|
| 优秀     | 超出预期，完美回答         | 0.9000~1.0000           | 1. 范畴100%匹配（A=0.4000）；2. 无遗漏（C_cover=K）、无多答（B基础分=0.3000）；3. 无任何错误（C_acc=0.2000）；4. 表达满分（D=0.1000）+ 补充强相关有效信息（如原理/实操细节） |
| 良好     | 符合预期，正确回答         | 0.7500~0.8999           | 1. 范畴≥90%匹配（A≥0.3600）；2. 无遗漏（C_cover=K）、无多答（B≥0.2500）；3. 无错误/仅1处轻微错误（C_acc≥0.1500）；4. 表达≥0.0800（至少满足结构+逻辑） |
| 合格     | 基本符合，少量瑕疵         | 0.5000~0.7499           | 1. 范畴≥80%匹配（A≥0.3200）；2. 遗漏≤2个关键信息（B≥0.2000）/ 多答≤2处（B≥0.2400）；3. 仅1处明显错误/≤2处轻微错误（C_acc≥0.0500）；4. 表达≥0.0500（至少满足结构清晰） |
| 较差     | 不符合预期，较多瑕疵       | 0.3000~0.4999           | 1. 范畴≥50%匹配（A≥0.2000）；2. 遗漏≥3个关键信息（B≤0.1500）/ 多答≥3处（B≤0.2100）；3. ≥1处明显错误/≤1处严重错误（C_acc≤0.0000）；4. 表达≤0.0400（仅满足1项表达要求） |
| 不合格   | 完全不符合，无效回答       | 0.0000~0.2999           | 1. 范畴<50%匹配（A<0.2000）；2. 严重错误≥1处（C_acc≤-0.0500）；3. 未覆盖任何关键信息（C_cover=0）；4. 表达≤0.0300（无结构、逻辑混乱） |

### 2. 映射规则（严格执行，消除模糊）
- 先计算“分项最终总分”（A+B+C_acc+D，约束在0~1），保留4位小数；
- 根据总分落入的“唯一分数范围”，直接对应等级，不允许跨范围（如0.7499→合格，0.7500→良好）；
- 特殊场景处理：若总分因“高敏感问题C_acc<0”被约束≤0.3000，直接映射为“不合格”。

## 四、关键注意事项
0. 若A=0.0000（范畴比例0%），直接判“总分0.0000+等级不合格”；
1. 关键信息点拆分原则：
   - 参考答案中明确列出的独立条目均算关键信息点；
   - 复合信息（如"周静负责，2024.5.31完成"）拆分为2个关键信息点；
   - 数值范围（如"卡接量≥4mm，夹角<50°"）拆分为2个关键信息点；
2. **覆盖判定严格性：**
   - **必须逐字逐句对比生成答案与参考答案，确保C_cover计数准确；**
   - **对于生成答案中的模糊或合并表述，若未明确包含参考答案的关键信息，视为遗漏；**
3. 分数计算规则：所有分项得分必须严格按公式计算，等级映射必须按“分数范围”执行，不允许主观调整（如0.7499不得人为升级为良好）；
4. 四舍五入规则：所有分数计算步骤（包括基础分、扣分、分项得分、最终总分）均保留4位小数（四舍五入）；
5. 总分约束：最终总分 = min(max(A + B + C_acc + D, 0.0000), 1.0000)（A=0时直接取0.0000）；
6. **无依据信息判定（严格化）：**
   - 参考答案中未提及的任何信息，生成答案中若有，均按"错误添加无依据信息"在B项扣分；
   - 同一句/段中多个无依据事实，按事实点数量拆分扣分；
   - 即使无依据信息本身正确（但与问题不相关或超出参考答案范围），仍要扣分；
7. **准确性评估一致性规则：**
   - 相同错误不重复扣分：同一要点中同一错误（如数值错误）只扣一次分，不按多次计算；
   - **乱码处理：乱码仅在C_acc中作为轻微错误扣分（每处0.0500），不在D项中重复扣分；**
   - 明确错误分类：对于数值错误，必须使用偏差公式计算，确保分类一致；
   - 乱码处理标准化：连续乱码字符作为一个错误，不按字符数拆分；
8. **评估严格性要求：**
   - 对于问题"请描述一下减震开发的工作基本点"类问题，必须严格检查是否遗漏关键步骤（如"手工样件试制"）；
   - 对于问题"FC-3AC缓冲块脱落..."类问题，必须检查是否添加了无依据信息（如批次统计表格）；
   - 确保前后评估标准一致，避免因评估者主观因素导致差异。
9. 所有扣分项必须在reason中明确标注具体错误/多余信息内容，禁止仅标注数量而不说明具体内容。

## 五、输出格式强制约束
1. 仅输出1个完整JSON对象，无任何多余内容（包括无额外空行、无注释、无说明文字）；
2. "score"字段数值必须与reason中"最终总分="后的数值完全一致（精确到4位小数），"level"字段必须与"等级="后的等级名称一致；
3. reason字段格式：
   - A=0时：必须包含"因范畴比例0%（A=0.0000），直接判总分0.0000，等级=不合格"，且B/C_acc/D标注"无需计算"；
   - A≠0时：必须完整计算B/C_acc/D，完整映射等级，无"无需计算"表述；
   - **C_acc部分必须严格按照"错误类型=[类型1(数量), 类型2(数量), ...]，错误数量=[总数]"格式输出；**
   - **B部分必须明确说明扣分明细：如"扣分=0.1400（遗漏关键信息点2处:0.1000, 错误添加无依据信息1处:0.0300, ...）"；**
   - **必须包含"等级映射依据：总分X.XXXX落入X.XXX~X.XXX范围，且满足[核心判定依据摘要，如无遗漏无错误]→等级=XX"；**
   - 所有扣分明细必须包含具体内容，而非仅标注数量，示例：
    - 完整性扣分："扣分=0.0900（错误添加无依据信息3处：1.材料配方不合理；2.结构设计缺陷；3.工艺问题（浇注成型不良），0.0900）"；
    - 准确性扣分："错误类型=[轻微错误(1), 明显错误(1)]，错误数量=2；具体错误内容：1.轻微错误：'缓冲块壁厚过薄'后出现乱码'（如断头锛夛紝'；2.明显错误：将'外观/制造缺陷'拆分为'外观设计缺陷'并误将'腰部断裂'归入，参考答案中无此分类；C_acc=0.0500分"；
4. 所有数值格式：比例保留2位小数，分数保留4位小数，无多余空格；
5. 禁止出现：JSON语法错误（如引号不闭合、逗号多余）、多余换行、空格冗余。

## 六、输出格式（仅JSON，无其他字符）
{% raw %}
{
  "reason": "1.范畴：目标=[提取的目标类型]，N=[总要点数]，M=[有效要点数]，比例=[范畴比例]%，A=[范畴得分]分；2.完整性：K=[关键信息点数]，C_cover=[覆盖数]，覆盖比例=[round((C_cover/K)*100,2)]%，基础分=[基础分]，扣分=[总扣分]（如：0.0900（错误添加无依据信息3处：1.材料配方不合理；2.结构设计缺陷；3.工艺问题（浇注成型不良），0.0900）），B=[完整性得分]分；3.准确性：错误类型=[具体错误类型]，错误数量=[具体数量]；具体错误内容：[每处错误的具体描述，如1.轻微错误：'缓冲块壁厚过薄'后出现乱码'（如断头锛夛紝'；2.明显错误：将'外观/制造缺陷'拆分为'外观设计缺陷'并误将'腰部断裂'归入，参考答案中无此分类]，C_acc=[准确性得分]分；4.表达：D=[表达得分]分；最终总分=min(max(A+B+C_acc+D,0.0000),1.0000)=[X.XXXX]；等级映射依据：总分[X.XXXX]落入[X.XXX~X.XXX]范围，且满足[核心判定依据摘要]→等级=[等级名称]"
  "score": [若A=0则填0.0000，否则填reason中最终总分数值，保留4位小数],
  "level": [若A=0则填"不合格"，否则填reason中等级名称]
}
{% endraw %}"""
